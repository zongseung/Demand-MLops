apiVersion: 1

providers:
  - name: 'Demand Dashboards'
    orgId: 1
    folder: ''
    folderUid: ''
    type: file
    disableDeletion: true      # 삭제 방지
    updateIntervalSeconds: 30
    allowUiUpdates: false      # UI에서 수정 차단
    options:
      path: /var/lib/grafana/dashboards
